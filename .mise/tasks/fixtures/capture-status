#!/usr/bin/env bash
# [MISE] description="Regenerate the recorded dry-run Status fixture corpus in internal/validate/testdata from a fresh k3s container (requires Docker)"
# [MISE] dir="{{config_root}}"
#
# Deliberately uncached (no sources/outputs): regeneration must always talk to
# a real cluster — the fixtures are raw server bytes, and only the server can
# say what they are. See CONTRIBUTING.md — Fixture corpus.
set -euo pipefail

go run ./hack/capture-status-fixtures
