# Locally built plugin binary (go build ./cmd/kubectl-craft)
/kubectl-craft

# Test artifacts
*.test
coverage.out

# macOS Finder metadata, wherever it lands
**/.DS_Store

# goreleaser output (snapshot and release builds)
/dist/

# Rendered demo recording (mise run demo:render) — published with releases,
# not committed: it re-renders on every polish pass and outweighs the
# repo's 500KB large-file cap. demo.tape is the source of truth.
/demo.gif

# Scratch dir the demo render bind mounts into the vhs container (must sit under
# the repo so Docker Desktop shares it); removed on exit, ignored if orphaned.
/.demo-render.*
